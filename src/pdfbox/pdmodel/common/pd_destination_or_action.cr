# Interface for elements that can be either a Destination or an Action
# Corresponds to org.apache.pdfbox.pdmodel.common.PDDestinationOrAction in Apache PDFBox
module Pdfbox::Pdmodel::Common
  module PDDestinationOrAction
    include COSObjectable
    # Empty interface - marker for objects that can be either a destination or an action
  end
end
